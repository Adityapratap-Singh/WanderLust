<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WonderLust</title>
</head>
<body>
    <h1>Listin details of --&gt; <%= foundListing.title %></h1>
    <ul>
        <li><h3><%= foundListing.title %></h3></li>
        <li><%= foundListing.description %></li>
        <li>Price: &#x20b9;<%= foundListing.price.toLocaleString("en-IN") %></li>
        <li>Location: <%= foundListing.location %></li>
        <li>
            <img src="<%= foundListing.image %>" alt="">
        </li>
        <li>Country: <%= foundListing.country %></li>
    </ul>
    <a href="/listings">  &lt;--Back to all listings</a>
    <a href="/listings/<%= foundListing._id %>/edit">
        <button type="button">Edit Listing</button>
    </a>
    <form action="/listings/<%= foundListing._id %>?_method=DELETE" method="POST" style="display:inline;">
        <button type="submit" onclick="return confirm('Are you sure you want to delete this listing?');">
            Delete Listing
        </button>
    </form>
</body>
</html>